import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onemealonehope.app',
  appName: 'One Meal One Hope',
  webDir: 'out',
};

export default config;